import React from 'react';

const PauseResume = (props) => {
    const id=props.timerid
    console.log('id -',id)
    return (
        <div>
           
        </div>
    );
};

export default PauseResume;